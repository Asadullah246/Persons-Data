import React from 'react';

const Loading = () => {
    return (
        <div>
            <h4 className='text-primary text-xl font-semibold'>Loading...</h4>
            
        </div>
    );
};

export default Loading;